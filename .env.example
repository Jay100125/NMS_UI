# Leave empty (or unset) for local dev — the Vite dev server proxies relative
# /api requests to the backend (see vite.config.ts). Set to the full backend
# URL only in production builds, e.g. VITE_API_BASE=https://api.example.com
VITE_API_BASE=
